# Auto-generated Makefile for ACT Multi-Flow Management
# Generated by docker-compose-generator.py

.PHONY: help start stop restart logs status rebuild clean

help:
	@echo "ACT Multi-Flow Management"
	@echo ""
	@echo "Available commands:"
	@echo "  make start      - Start all flows"
	@echo "  make stop       - Stop all flows"
	@echo "  make restart    - Restart all flows"
	@echo "  make logs       - View logs from all flows"
	@echo "  make status     - Show status of all containers"
	@echo "  make rebuild    - Rebuild and restart all flows"
	@echo "  make clean      - Stop and remove all containers"
	@echo ""
# Individual flow commands:
# make start-math, stop-math, logs-math
# make start-restaurant, stop-restaurant, logs-restaurant
# make start-restaurant2, stop-restaurant2, logs-restaurant2
# make start-risk, stop-risk, logs-risk

start:
	@echo "🚀 Starting all flows..."
	docker-compose up -d

stop:
	@echo "🛑 Stopping all flows..."
	docker-compose stop

restart:
	@echo "🔄 Restarting all flows..."
	docker-compose restart

logs:
	docker-compose logs -f

status:
	@echo "📊 Flow Status:"
	@docker-compose ps

rebuild:
	@echo "🔨 Rebuilding all flows..."
	docker-compose down
	docker-compose up --build -d

clean:
	@echo "🧹 Cleaning up..."
	docker-compose down -v

start-math:
	docker-compose up -d act-math

stop-math:
	docker-compose stop act-math

restart-math:
	docker-compose restart act-math

logs-math:
	docker-compose logs -f act-math

start-restaurant:
	docker-compose up -d act-restaurant

stop-restaurant:
	docker-compose stop act-restaurant

restart-restaurant:
	docker-compose restart act-restaurant

logs-restaurant:
	docker-compose logs -f act-restaurant

start-restaurant2:
	docker-compose up -d act-restaurant2

stop-restaurant2:
	docker-compose stop act-restaurant2

restart-restaurant2:
	docker-compose restart act-restaurant2

logs-restaurant2:
	docker-compose logs -f act-restaurant2

start-risk:
	docker-compose up -d act-risk

stop-risk:
	docker-compose stop act-risk

restart-risk:
	docker-compose restart act-risk

logs-risk:
	docker-compose logs -f act-risk
