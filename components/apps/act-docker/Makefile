# Auto-generated Makefile for ACT Multi-Flow Management
# Generated by docker-compose-generator.py

.PHONY: help start stop restart logs status rebuild clean

help:
	@echo "ACT Multi-Flow Management"
	@echo ""
	@echo "Available commands:"
	@echo "  make start      - Start all flows"
	@echo "  make stop       - Stop all flows"
	@echo "  make restart    - Restart all flows"
	@echo "  make logs       - View logs from all flows"
	@echo "  make status     - Show status of all containers"
	@echo "  make rebuild    - Rebuild and restart all flows"
	@echo "  make clean      - Stop and remove all containers"
	@echo ""
# Individual flow commands:
# make start-action-builder, stop-action-builder, logs-action-builder
# make start-iss-location, stop-iss-location, logs-iss-location

start:
	@echo "🚀 Starting all flows..."
	docker-compose up -d

stop:
	@echo "🛑 Stopping all flows..."
	docker-compose stop

restart:
	@echo "🔄 Restarting all flows..."
	docker-compose restart

logs:
	docker-compose logs -f

status:
	@echo "📊 Flow Status:"
	@docker-compose ps

rebuild:
	@echo "🔨 Rebuilding all flows..."
	docker-compose down
	docker-compose up --build -d

clean:
	@echo "🧹 Cleaning up..."
	docker-compose down -v

start-action-builder:
	docker-compose up -d act-action-builder

stop-action-builder:
	docker-compose stop act-action-builder

restart-action-builder:
	docker-compose restart act-action-builder

logs-action-builder:
	docker-compose logs -f act-action-builder

start-iss-location:
	docker-compose up -d act-iss-location

stop-iss-location:
	docker-compose stop act-iss-location

restart-iss-location:
	docker-compose restart act-iss-location

logs-iss-location:
	docker-compose logs -f act-iss-location
