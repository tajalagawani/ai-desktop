# Auto-generated docker-compose.yml for ACT Multi-Flow
# Generated by docker-compose-generator.py
# DO NOT EDIT MANUALLY - Run ./startup.sh to regenerate

services:

  act-math:
    build: .
    container_name: act-math
    ports:
      - "9000:9000"
    environment:
      - PYTHONUNBUFFERED=1
      - ACT_PORT=9000
    volumes:
      - ./act:/app/act
      - ./flows/math.flow:/app/flow
    restart: unless-stopped
    networks:
      - act-network

  act-restaurant:
    build: .
    container_name: act-restaurant
    ports:
      - "5544:5544"
    environment:
      - PYTHONUNBUFFERED=1
      - ACT_PORT=5544
    volumes:
      - ./act:/app/act
      - ./flows/restaurant.flow:/app/flow
    restart: unless-stopped
    networks:
      - act-network

  act-restaurant2:
    build: .
    container_name: act-restaurant2
    ports:
      - "8081:8081"
    environment:
      - PYTHONUNBUFFERED=1
      - ACT_PORT=8081
    volumes:
      - ./act:/app/act
      - ./flows/restaurant2.flow:/app/flow
    restart: unless-stopped
    networks:
      - act-network

  act-risk:
    build: .
    container_name: act-risk
    ports:
      - "8088:8088"
    environment:
      - PYTHONUNBUFFERED=1
      - ACT_PORT=8088
    volumes:
      - ./act:/app/act
      - ./flows/risk.flow:/app/flow
    restart: unless-stopped
    networks:
      - act-network

networks:
  act-network:
    driver: bridge
