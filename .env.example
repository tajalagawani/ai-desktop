# ==================================
# AI Desktop - Environment Variables
# ==================================

# -----------------
# App Configuration
# -----------------
NEXT_PUBLIC_APP_NAME="AI Desktop"
NEXT_PUBLIC_APP_URL=http://localhost:3000

# For VPS deployment, set this to your domain:
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# -----------------
# Database (Future)
# -----------------
# When you add database support, uncomment and configure:
# DATABASE_URL=postgresql://user:password@localhost:5432/aidesktop
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# -----------------
# Authentication (Future)
# -----------------
# When you implement real auth with NextAuth.js:
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32

# -----------------
# OAuth Providers (REQUIRED for ACP Agent)
# -----------------
# GitHub OAuth (for GitHub MCP server)
# Setup: https://github.com/settings/developers → New OAuth App
# Callback: http://YOUR_VPS_IP/api/oauth/github/callback
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# GitLab OAuth (for GitLab MCP server)
# Setup: https://gitlab.com/-/profile/applications
# Callback: http://YOUR_VPS_IP/api/oauth/gitlab/callback
GITLAB_CLIENT_ID=your-gitlab-client-id
GITLAB_CLIENT_SECRET=your-gitlab-client-secret

# Google OAuth (for Google Drive/Gmail/Calendar MCP servers)
# Setup: https://console.cloud.google.com/apis/credentials
# Callback: http://YOUR_VPS_IP/api/oauth/google/callback
# Scopes: https://www.googleapis.com/auth/drive, https://www.googleapis.com/auth/gmail.readonly, https://www.googleapis.com/auth/calendar
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Slack OAuth (for Slack MCP server)
# Setup: https://api.slack.com/apps → Create New App
# Callback: http://YOUR_VPS_IP/api/oauth/slack/callback
# Scopes: channels:history, channels:read, chat:write, users:read
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret

# Stripe OAuth (for Stripe MCP server)
# Setup: https://dashboard.stripe.com/settings/applications
# Callback: http://YOUR_VPS_IP/api/oauth/stripe/callback
STRIPE_CLIENT_ID=your-stripe-client-id
STRIPE_CLIENT_SECRET=your-stripe-client-secret

# -----------------
# AI/ML Services (Future)
# -----------------
# OpenAI (for AI assistant and workflows)
# OPENAI_API_KEY=sk-your-openai-api-key

# -----------------
# Redis (Future - for sessions)
# -----------------
# REDIS_URL=redis://localhost:6379

# -----------------
# Email Service (Future)
# -----------------
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@your-domain.com

# -----------------
# Monitoring & Analytics
# -----------------
# Vercel Analytics (already integrated)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# Sentry (for error tracking - future)
# SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=

# -----------------
# Security
# -----------------
# Rate limiting (future)
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW=60000

# CORS allowed origins (future)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# -----------------
# Development
# -----------------
# Set to 'development' locally, 'production' on VPS
NODE_ENV=development

# Enable debug logs (future)
# DEBUG=false

# -----------------
# VPS Deployment
# -----------------
# Port for the app to listen on
PORT=3000

# PM2 instances (0 = max CPUs)
# PM2_INSTANCES=0
